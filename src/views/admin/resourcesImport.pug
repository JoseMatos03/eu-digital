extends ../layouts/layout

block content
  h2 Importar SIP
  form(method="post", action="http://localhost:3001/api/admin/resources", enctype="multipart/form-data", data-ajax, data-redirect="/admin/resources")
    div
      label Escolher ficheiro ZIP
      input(type="file", name="sip")
    button(type="submit") Upload
