{
  "name": "eu-digital",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "jest --runInBand",
    "dev": "nodemon --watch . --ext js,pug --exec \"node server.js\"",
    "clean:fs": "rimraf uploads/* tmp_sips/* logs/*",
    "clean:docker": "docker compose down -v",
    "clean": "npm run clean:docker && npm run clean:fs",
    "fresh": "npm run clean && docker compose up --build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "archiver": "^7.0.1",
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "fs-extra": "^11.3.0",
    "mongoose": "^8.13.2",
    "multer": "^1.4.5-lts.2",
    "pug": "^3.0.3",
    "unzipper": "^0.12.3",
    "winston": "^3.17.0",
    "winston-daily-rotate-file": "^5.0.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.1.9",
    "rimraf": "^6.0.1",
    "supertest": "^7.1.0"
  }
}
